# Automatic review smoke test

This documentation-only change verifies that opening an eligible pull request triggers the configured automatic review workflow.

The PR intentionally stays small so it remains below the repository's changed-line limit.
